import React from 'react'
import ReactDOM from 'react-dom/client'
import { App } from './App'

// Isso aqui renderiza a main

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
)
